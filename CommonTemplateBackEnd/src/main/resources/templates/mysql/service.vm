package [(${unit.packageName})].service;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import [(${unit.projectPackageName})].common.base.BaseService;
import [(${unit.packageName})].mapper.[(${table.javaName})]Mapper;
import [(${unit.packageName})].entity.[(${table.javaName})];
/**  
* @Title: [(${table.javaName})].java  
* @Package [(${unit.packageName})].entity  
* @Description: TODO([(${table.javaName})]的service)
* @author System-Auto-Generate
* @date [(${generateDate})]
* @version V1.0  
*/
@Service
@Transactional(rollbackFor = Exception.class)
public class  [(${table.javaName})]Service extends BaseService<[(${table.javaName})]> {
    @Autowired
    private [(${table.javaName})]Mapper coreMapper;
}